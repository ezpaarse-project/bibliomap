version: "3.3"

services:
  enricher:
    build:
      context: ./services/enricher
    container_name: bibliomap-enricher

  player:
    build:
      context: ./services/player
    container_name: bibliomap-player

  viewer:
    build:
      context: ./services/viewer
    container_name: bibliomap-viewer
  
  nginx:
    build:
      context: ./services/nginx
    container_name: bibliomap-nginx

